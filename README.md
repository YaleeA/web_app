1. create a Flask app with two endpoints

2. build and push the image to docker hub 
        docker login
        docker build -t yaleea1996/web_app .
        docker push yaleea1996/web_app

3. terraform + docker to deploy locally - 